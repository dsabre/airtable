name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1.1.0
    - name: Run a multi-line script
      run: |
        npm i,
        npm run build,
        cd dist/airtable/
        ls -l.
